{
  "name": "kopfmathe",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-static.mjs",
    "build": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
